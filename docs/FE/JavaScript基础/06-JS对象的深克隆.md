```js
function deepClone(target) {
    // 使用hash解决循环引用的问题
    const map = new Map()
    function clone(target) {
        if(isObject()) {

        } else {
            return target;
        }
    }
}
```